#include<stdio.h>

int main() {

  int N1, N2;
  int sum;
  
  scanf("%d %d", &N1, &N2);  //利用scanf讀入兩數字
  //printf("N1: %d, N2: %d\n", N1, N2);
  
  sum = N1 + N2;
  printf("SUM: %d\n", sum);
  return 0;
  
}
